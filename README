Madeo TODOs app
===============

$ npm install
$ composer install
$ bower install
$ grunt

$ vim Config/database.php # edit db credentials
$ mysql -u user -p -e madeo < schema.sql
$ apache vhost is located ad Config/Vhost/local.madeo.conf

open http://local.madeo/users/add

enjoy!
